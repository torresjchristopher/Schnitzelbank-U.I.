{
  "mcpServers": {
    "local-filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "./src",
        "./docs"
      ]
    }
  },
  "mcp": {
    "allowed": ["local-filesystem"],
    "serverCommand": "stdio"
  }
}